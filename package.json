{
  "name": "e2e-todo-app",
  "version": "1.0.0",
  "description": "E2E Todo App REST API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "pretest": "test -x node_modules/.bin/jest || NODE_ENV=development npm install --include=dev",
    "test": "jest --verbose --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
